# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.output
stats.html
stats-*.json
.wxt
web-ext.config.ts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
_stashed_content.ts
_stashed_reddit-inline.css
/wxt-main

# Exclude temp/build JSON files but keep config files
stats-*.json
/internal
repomix-output.xml
.env
.env.*
!.env.example
# Build config, not secrets: sets NODE_ENV=development so `--mode
# development` builds get import.meta.env.DEV. Without it a fresh clone's
# dev build silently loses every DEV-gated affordance.
!.env.development
.env.submit.backup-*
KOMENTOSCRIPT.md
/.claude
komentoscript-pack.schema.json
/docs/
